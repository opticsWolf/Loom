[package]
name = "navette-color"
version = "0.2.0"
edition = "2024"
description = "Pure-Rust color engine core (no Python, no I/O). Bindings live in navette-color-py."
license = "LGPL-3.0-or-later"

[lib]
name = "navette_color"
crate-type = ["rlib"]

[dependencies]
rayon = { workspace = true }
