[package]
name = "navette-smatrix"
version = "0.3.0"
edition = "2024"
description = "Pure-Rust S-matrix thin-film engine core (no Python). Bindings live in navette-smatrix-py."
license = "LGPL-3.0-or-later"

[lib]
name = "navette_smatrix"
crate-type = ["rlib"]

[dependencies]
num-complex = { workspace = true }
rayon = { workspace = true }
