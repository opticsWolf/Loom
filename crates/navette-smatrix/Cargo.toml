[package]
name = "navette-smatrix"
version = "0.4.0"
edition = "2024"
description = "Pure-Rust S-matrix thin-film engine core (no Python).."
license = "LGPL-3.0-or-later"
authors = ["opticsWolf"]
repository = "https://github.com/opticsWolf/Navette"

[lib]
name = "navette_smatrix"
crate-type = ["rlib"]

[dependencies]
navette-structure = { workspace = true }
num-complex = { workspace = true }
rayon = { workspace = true }
